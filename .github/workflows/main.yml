name: Python application

on:
  push:
    branches: [ master, develop ]
  pull_request:
    branches: [ master, develop ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['2.7', '3.7', '3.8', '3.9']

    steps:
    - uses: actions/checkout@v2

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        sudo apt-get -qq update
        sudo apt-get install -y libemail-outlook-message-perl
        pip install ".[dev, test]"
        export PERL_MM_USE_DEFAULT=1
        sudo cpan -f -i Email::Outlook::Message

    - name: Run tests
      run: |
        pytest
        python -m mailparser -v
        python -m mailparser -h
        mail-parser -f tests/mails/mail_malformed_3 -j
        cat tests/mails/mail_malformed_3 | mail-parser -k -j

#    - name: Report to Coveralls
#      uses: AndreMiras/coveralls-python-action@v20201113
#      with:
#        github-token: ${{ secrets.GITHUB_TOKEN }}
#        coveralls-token: ${{ secrets.COVERALLS_TOKEN }}
#
#    - name: Build and push Docker image
#      if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/develop'
#      run: |
#        docker build --build-arg BRANCH=${{ github.ref }} -t $DOCKER_USERNAME/spamscope-mail-parser .
#        echo $DOCKER_PASSWORD | docker login -u $DOCKER_USERNAME --password-stdin
#        docker push $DOCKER_USERNAME/spamscope-mail-parser